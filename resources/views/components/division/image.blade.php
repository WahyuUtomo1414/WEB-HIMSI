@props(['division'])

<section class="rounded-3xl overflow-hidden border border-slate-200 shadow-xl max-h-[450px]">
    <img src="{{ $division['image_url'] }}" alt="{{ $division['name'] }}" class="h-full w-full object-cover">
</section>
